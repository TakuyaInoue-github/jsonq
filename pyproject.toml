[project]
name = "jsonq"
version = "0.1.0"
description = "a jq-like JSON exploration experience tools."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=67"]
build-backend = "setuptools.build_meta"
